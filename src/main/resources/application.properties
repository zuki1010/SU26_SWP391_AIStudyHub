spring.application.name=SU26_SWP391_AIStudyHub

# ===================================================================
# DATABASE CONFIGURATION (SUPABASE POSTGRESQL - PORT 6543)
# ===================================================================
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0&stringtype=unspecified
spring.datasource.username=postgres.ybgeblpkptrsefpafthb
spring.datasource.password=DHRK7cgKgqi3K8MS
spring.datasource.driver-class-name=org.postgresql.Driver


spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=300000

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.sql.init.mode=never

# ===================================================================
# TẮT XUNG ĐỘT AWS S3 AUTOCONFIGURATION (QUAN TRỌNG)
# ===================================================================
spring.cloud.aws.s3.enabled=false
spring.cloud.aws.region.auto=false
spring.cloud.aws.credentials.instance-profile=false

# ===================================================================
# THÔNG TIN XÁC THỰC VÀ BẢO MẬT
# ===================================================================
spring.thymeleaf.check-template-location=false
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

app.jwt.secret=ChangeMeInApplicationLocalPropertiesMustBeAtLeast256BitsLongForHS256!!
app.jwt.access-expiration-ms=604800000
app.jwt.refresh-expiration-ms=604800000
app.jwt.reset-expiration-ms=3600000

app.frontend.reset-password-url=http://localhost:5173/reset-password
app.mail.from=noreply@aistudyhub.local

springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method


# ===================================================================
# CONFIGURATION FOR CLOUD STORAGE (SUPABASE REST API)
# ===================================================================
supabase.url=${SUPABASE_URL}
supabase.bucket-name=documents
#
## Gọi chính xác tên biến từ file .env mới chỉnh sửa
supabase.anon-key=${SUPABASE_ANON_KEY}
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY}

# Đọc key Gemini chuẩn chỉnh (xóa dấu cách thừa sau dấu bằng)
gemini.api.key=${GEMINI_API_KEY}


# Upload file size config
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=60MB

# Tomcat config for large multipart request
server.tomcat.max-swallow-size=-1
server.tomcat.max-http-form-post-size=60MB

spring.cloud.aws.credentials.access-key=${SUPABASE_S3_ACCESS_KEY}
spring.cloud.aws.credentials.secret-key=${SUPABASE_S3_SECRET_KEY}
spring.cloud.aws.region.static=${SUPABASE_S3_REGION}
spring.cloud.aws.s3.endpoint=${SUPABASE_S3_ENDPOINT}


spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=aistudyhub817@gmail.com
spring.mail.password=xrywaqriavobthuz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.frontend-url=http://localhost:5173
app.mail.from=aistudyhub817@gmail.com

# ===================================================================
# TECHCOMBANK QR PAYMENT
# ===================================================================
payment.bank-code=TCB
payment.bank-name=Techcombank
payment.account-number=19038500362012
payment.account-name=NGUYEN CHI CUONG

# Demo 0đ thì để 0.
# Muốn có giao dịch ngân hàng thật thì để 1000 hoặc 2000.
payment.amount=${PAYMENT_AMOUNT:0}

# Giá gói hiển thị trên giao diện.
payment.display-price=${PAYMENT_DISPLAY_PRICE:99000}